apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: keycloak
spec:
  selector:
    app: keycloak
  type: NodePort
  ports:
    - port: 8443
      targetPort: 8443
      nodePort: 30900
      protocol: TCP
