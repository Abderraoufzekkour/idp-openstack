apiVersion: compute.openstack.crossplane.io/v1alpha1
kind: Instance
metadata:
  name: test-idp-vm
spec:
  forProvider:
    flavorName: m1.small
    imageName: ubuntu-22.04
    networks:
      - name: public
    securityGroups:
      - name: default
  providerConfigRef:
    name: openstack-provider
